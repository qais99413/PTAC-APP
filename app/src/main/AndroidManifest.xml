<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PTACAPPLIACTION"
        tools:targetApi="33" >

        <activity android:name=".activities.FeedbackActivity"
            android:exported="true"/>

        <activity android:name=".activities.ViewSchMileageHistoryActivity"
            android:exported="true"/>

        <activity android:name=".activities.ViewMaintenanceHistoryActivity"
            android:exported="true"/>

        <activity android:name=".activities.ViewRoadAssistHistoryActivity"
            android:exported="true"/>

        <activity android:name=".activities.ViewOilChangeHistoryActivity"
            android:exported="true"/>

        <activity android:name=".activities.MaintenanceAppointmentActivity"
            android:exported="true"/>

        <activity android:name=".activities.RoadAssistActivity"
            android:exported="true"/>

        <activity android:name=".activities.ViewOilServicesActivity"
            android:exported="true"/>

        <activity
            android:name=".activities.SignUpActivity"
            android:exported="true" />

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true" />

        <activity android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>